<table class="table table-hover">
    <p>
    <strong><%= Project.model_name.human(count: projects.count) %>:</strong>
    </p>
  <thead>
    <tr>
      <th><%= "Nome" %></th>
      <th><%= "Valor" %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% projects.each do |project| %>
      <tr>
        <td><%= project.name %></td>
        <td ><%= project.value %></td>
      </tr>
    <% end %>
  </tbody>
</table>