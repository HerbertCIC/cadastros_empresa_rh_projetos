<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <title>Autenticação</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag "authentication", media: "all", 'data-turbolinks-track': "reload" %>
    <%= javascript_pack_tag "authentication", 'data-turbolinks-track': "reload" %>
  </head>

  <body class="bg-dark">

    <div class='d-flex h-100 align-items-center justify-content-center row'>
      <main class='col-8 col-lg-6 shadow p-5 my-5 bg-white rounded'>
        <% flash.each do |type, message| %>
              <div class="alert alert-<%= type == "alert" ? "danger" : "success" %> alert-dismissible fade show" role="alert">
                <%= message %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
              </div>
        <% end %>
        <%= render "devise/shared/error_messages", resource: resource %>
        <div class="border rounded mb-3">
          <div class="p-3 col-md-6 offset-md-3">
            
            <%= yield %>
          </div>
        </div>

      </main>
    </div>   
    

  </body>

</html>
